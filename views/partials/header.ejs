<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Affordable, reliable copper installation in Columbia, Missouri. Schedule online or explore the developer toolkit.">
  <meta property="og:title" content="Action Jackson Installs" />
  <meta property="og:description" content="Professional installations & the software toolkit behind the service." />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://actionjacksoninstalls.com/" />
  <meta property="og:image" content="https://actionjacksoninstalls.com/preview.png" />
  <% 
  const defaultPageTitle = variant === 'portfolio' ? 'Developer Hub' : 'Installer Site';
  const pageTitle = title || defaultPageTitle;
  const siteName = variant === 'portfolio' ? 'Action Jackson Installs Toolkit' : 'Action Jackson Installs';
  %>
  <title><%= pageTitle %> | <%= siteName %></title>

  <!-- Pico CSS -->
  <link
    rel="stylesheet"
    href="https://unpkg.com/@picocss/pico@1.5.10/css/pico.min.css"
  />
  <!-- custom overrides -->
  <link rel="stylesheet" href="/styles/custom.css" />

  <!-- Font Awesome Kit  -->
  <script
    src="https://kit.fontawesome.com/8623ae8306.js"
    crossorigin="anonymous"
  ></script>
</head>
<body>
  <header>
    <nav class="container">
      <a href="<%= variant === 'portfolio' ? '/' : '/' %>" class="contrast brand">
        <% if (variant === 'portfolio') { %>
          Action Jackson Installs Toolkit
        <% } else { %>
          Action Jackson Installs
        <% } %>
      </a>
      <ul>
        <% if (variant === 'business') { %>
          <li><a href="/">Home</a></li>
          <li><a href="/about">About</a></li>
          <li><a href="/scheduling">Schedule</a></li>
          <li><a href="/quotes" class="contrast">Get a Quote</a></li>
        <% } else { %>
          <!-- Portfolio / developer nav -->
          <li><a href="/">Developer Hub</a></li>
          <a href="https://github.com/DaSonOfPoseidon" target="_blank" rel="noopener noreferrer">GitHub</a>
          <li><a href="https://actionjacksoninstalls.com">Installer Site</a></li>
        <% } %>
      </ul>
    </nav>

    <div class="site-switcher" style="padding:6px 0; text-align:center;">
      <% if (variant === 'business') { %>
        <a href="<%= switcherUrl %>" aria-label="View Developer Hub">
          Looking for the software engineering portfolio? View Developer Hub →
        </a>
      <% } else { %>
        <a href="<%= switcherUrl %>" aria-label="Go to Installer Site">
          Need installations or a quote? Go to Installer Site →
        </a>
      <% } %>
    </div>
  </header>
