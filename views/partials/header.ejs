<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Affordable, reliable copper installation in Columbia, Missouri. Schedule online or explore the developer toolkit.">
  <meta property="og:title" content="Action Jackson Installs" />
  <meta property="og:description" content="Professional installations & the software toolkit behind the service." />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://actionjacksoninstalls.com/" />
  <meta property="og:image" content="https://actionjacksoninstalls.com/preview.png" />
  <% 
  const defaultPageTitle = variant === 'portfolio' ? 'Action Jackson Builds' : 'Action Jackson Installs';
  const pageTitle = title || defaultPageTitle;
  const siteName = variant === 'portfolio' ? 'Action Jackson Builds' : 'Action Jackson Installs';
  %>
  <title><%= pageTitle %> | <%= siteName %></title>

    <!-- Base styles & fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap"
    rel="stylesheet"
  />
  <!-- custom overrides -->
  <link rel="stylesheet" href="/styles/base.css">
  <link rel="stylesheet" href="/styles/<%= variant === 'portfolio' ? 'software' : 'business' %>.css">

  <!-- Font Awesome Kit  -->
  <script
    src="https://kit.fontawesome.com/8623ae8306.js"
    crossorigin="anonymous"
  ></script>
</head>
<body>
  <header>
    <nav class="container">
      <a href="/" class="contrast brand">
        <% if (variant === 'portfolio') { %>
          Action Jackson Builds 
        <% } else { %>
          Action Jackson Installs
        <% } %>
      </a>
      <ul>
        <% if (variant === 'business') { %>
          <li><a href="/">Home</a></li>
          <li><a href="/about">About</a></li>
          <li><a href="/scheduling">Schedule</a></li>
          <li><a href="/quotes" class="contrast">Get a Quote</a></li>
        <% } else { %>
          <!-- Portfolio / developer nav -->
          <li><a href="/">Action Jackson Builds</a></li>
          <li><a href="https://www.linkedin.com/in/jackson-keithley-115582213/" target="_blank" rel="noopener noreferrer">LinkedIn</a></li>
          <li><a href="https://github.com/DaSonOfPoseidon" target="_blank" rel="noopener noreferrer">GitHub</a></li>
          <li><a href="https://actionjacksoninstalls.com">Action Jackson Installs</a></li>
        <% } %>
      </ul>
    </nav>
  </header>
